name: iOS Build
on:
  push:
    branches: [ main ]
    paths:
      - 'ios/**'
      - '.github/workflows/ios-build.yml'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build iOS App
    runs-on: macos-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Select Xcode
        run: sudo xcode-select -switch /Applications/Xcode.app/Contents/Developer
      
      - name: Show Xcode version
        run: xcodebuild -version
      
      - name: Install xcodegen
        run: brew install xcodegen
      
      - name: Generate Xcode project
        run: |
          cd ios
          xcodegen generate
      
      - name: Build for Simulator
        run: |
          cd ios
          xcodebuild build \
            -scheme ChumianBrowser \
            -destination 'platform=iOS Simulator,name=iPhone 15' \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=YES
      
      - name: Package .app
        run: |
          cd ios/build/Build/Products/Debug-iphonesimulator
          zip -r ChumianBrowser-simulator.zip ChumianBrowser.app
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: chumian-browser-ios-simulator
          path: ios/build/Build/Products/Debug-iphonesimulator/ChumianBrowser-simulator.zip
          retention-days: 90
      
      - name: Build for Generic iOS Device
        run: |
          cd ios
          xcodebuild build \
            -scheme ChumianBrowser \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build-device \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=NO
      
      - name: Package Device .app
        run: |
          cd ios/build-device/Build/Products/Debug-iphoneos
          zip -r Chumian-browser-device.zip ChumianBrowser.app
      
      - name: Upload Device Artifact
        uses: actions/upload-artifact@v4
        with:
          name: chumian-browser-ios-device
          path: ios/build-device/Build/Products/Debug-iphoneos/Chumian-browser-device.zip
          retention-days: 90
